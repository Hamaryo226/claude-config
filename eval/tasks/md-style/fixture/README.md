# csv-tools

期間を指定してCSVを書き出すためのツールです。

## 使い方

まず依存関係をインストールします。

```
npm install
```

つぎにビルドします。

* ビルド： `npm run compile`
* テスト： `npm run test`
* 書き出し： `npm run dump -- 2024-01-01 2024-03-31`

上記のコマンドで、期間ごとのCSVを書き出すことができます（出力先はカレントディレクトリです）。

## 注意

* Node18以上が必要です．
* 出力ファイルは上書きされます．
