# 個人設定 (全プロジェクト共通)

<!-- 200 行を超えると追従率が落ちる。増やしたくなったら rules/ か skills/ に逃がすこと。 -->

各リポジトリの `CLAUDE.md` / `AGENTS.md` / `.claude/settings.json` が常に優先。
ここに書くのは、どのプロジェクトでも変わらない前提だけ。

## 応答

- 日本語で答える。コード・識別子・エラーメッセージ・コマンドは原文のまま扱う
- 結論から書く。前置きと復唱はしない
- 不明点は推測で埋めず、確認するか「ここは未確認」と明示する
- 「直しました」と報告する前に、ビルドかテストか実行で必ず確認する。確認していないなら「未確認」と書く

## 環境

- Windows 11 / PowerShell 7 (pwsh) / Git Bash あり
- **`jq` と `rg` は入っていない**。JSON の加工は `node -e` か PowerShell の `ConvertFrom-Json`、検索は Grep ツールを使う
- 入っているもの: node, npm, pnpm, bun, python, uv, ruff, git, gh, dotnet, cargo, java, code
- **パスは `/` 区切りで書く**。Windows でも動くうえ、Git Bash 経由でバックスラッシュがエスケープとして食われる事故を防げる
- 作業リポジトリの置き場所: `C:/Users/hama/Documents/GitHub/`
- 一時ファイルはリポジトリ内に作らない。スクラッチ用ディレクトリを使う

## git

- `main` / `master` に直接コミットしない。作業ブランチを切る
- push は必ず確認を取ってから (許可プロンプトが出る設定にしてある)
- コミットは論理単位に分ける。無関係な変更を 1 コミットに混ぜない
- コミットメッセージは**日本語・1 行・prefix なし**で「何をどう変えたか」を書く
  - 例: `連打の終わりが無い譜面で落ちるのを直す` / `期間ごとのcsv,xlsxをエクスポートできる機能追加`
  - `feat:` `fix:` のような Conventional Commits 形式は使わない
- `git reset --hard` / `git clean -fdx` / `--force` push / `--no-verify` は使わない (フックで止まる)

## コード

- 既存コードのスタイル・命名・コメントの密度に合わせる。自分の好みを持ち込まない
- リポジトリに設定があるフォーマッタ・リンタに従う。設定が無いリポジトリを勝手に整形しない
- 依存の追加は最小限に。標準ライブラリか既存の依存で足りるならそれを使う
- 例外を握りつぶさない。握りつぶすなら理由をコメントに書く
- コメントは「なぜ」を書く。「何を」はコードで読める

## 作業の進め方

- 大きめの変更は先に方針を示す。細かい変更は聞かずに進める
- 調べ物が要るときは推測で書かず、実際にファイルを読むか公式ドキュメントを引く
- 途中で仕様の疑問が出たら、それに依存しない部分を先に片付けてから聞く
- 依頼された範囲を勝手に広げない。ついでに直したいものが見つかったら、報告だけして手は出さない
